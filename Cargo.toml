[package]
name = "example-rust-slint"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "1.15.1"

[build-dependencies]
slint-build = "1.15.1"

[profile.release]
strip = true
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"